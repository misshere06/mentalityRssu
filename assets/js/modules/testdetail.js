document.addEventListener('DOMContentLoaded', function() {
    const submitBtn = document.getElementById('submitTestBtn');
    if (submitBtn) {
        submitBtn.addEventListener('click', function() {
            alert('Функция сохранения ответов будет добавлена позже.');
        });
    }
});